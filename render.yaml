services:
  - type: web
    name: data-entry-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -b 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: "0"
      - key: DATABASE_URL               # ← ここがポイント
        fromDatabase:
          name: data-entry-db           # ← 下の databases の name と一致させる
          property: connectionString

databases:
  - name: data-entry-db
    plan: free
